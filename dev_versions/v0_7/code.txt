states = qsv(3, 0)
states.h()

@quantum
def add_one(state):
    return state + 1

print(add_one(1))
add_one(states)

y = qsv(3, 0)
forall state in states:
    y.store(qconst(3, 6) & state & qconst(3, 7))
